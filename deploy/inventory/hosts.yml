---
# Informacyjna mapa hostów dla orchestracji workspace (bez sekretów).
# Ansible inventory per warstwa pozostaje w repo domenowych.
all:
  children:
    t630_servers:
      hosts:
        t630:
          ansible_host: 192.168.1.20
    g2_servers:
      hosts:
        g2:
          ansible_host: 192.168.1.19
